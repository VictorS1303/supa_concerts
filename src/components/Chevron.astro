---
const {class: className, id: id} = Astro.props
---

<i class="fas fa-chevron-down absolute bottom-[10%] left-[50%] -translate-x-1/2 text-3xl text-white"></i>

<style>
  .fa-chevron-down
  {
    animation: animateChevron 1s linear infinite;
  }

  @keyframes animateChevron
  {
    50%
    {
      transform: translateY(20%);
    }
  }
</style>